name: DroneWorld
services:
  backend:
    image: droneworld/droneworld-backend:v1.0.0
    build:
      context: ./backend
      args:
        BACKEND_PORT: ${BACKEND_PORT:-5000}
    ports:
      - "${BACKEND_PORT:-5000}:${BACKEND_PORT:-5000}"
    volumes:
      - ./backend:/app  # Mount backend code
      - ./credentials:/app/credentials  # Mount credentials folder
      - ${USERPROFILE:-$HOME}/Documents/AirSim:/root/Documents/AirSim
    env_file:
      - ./backend/.env  # Load environment variables from .env file
    environment:
      # DRV-Unreal service connection
      # Debug can be enabled locally by setting FLASK_ENV/FLASK_DEBUG via .env; keep off by default
      - BACKEND_HOST=${BACKEND_HOST:-backend}
      - FLASK_RUN_PORT=${BACKEND_PORT:-5000}
      - DRV_UNREAL_HOST=drv-unreal
      - DRV_UNREAL_API_PORT=${SIM_INTERNAL_PORT:-3000}  # DRV API port (if available)
      - DRV_PIXELSTREAM_PORT=${PIXELSTREAM_PORT:-8888}
      - BACKEND_PORT=${BACKEND_PORT:-5000}
    networks:
      - droneworld-network

  frontend:
    image: droneworld/droneworld-frontend:v1.0.0
    build:
      context: ./frontend
      args:
        FRONTEND_PORT: ${FRONTEND_PORT:-3000}
    ports:
      - "${FRONTEND_PORT:-3000}:${FRONTEND_PORT:-3000}"  # Frontend port
    volumes:
      - ./frontend:/app  # Mount frontend code for live editing
      - /app/node_modules # Separate node_modules to avoid host overwrite
    environment:
      - CHOKIDAR_USEPOLLING=true  # Necessary for live-reload in Docker
      - CHOKIDAR_INTERVAL=1000  # Poll every 1 second
      - WATCHPACK_POLLING=true   # Webpack 5 polling
      - FAST_REFRESH=true        # Ensure fast refresh is on
      - FRONTEND_HOST=${FRONTEND_HOST:-frontend}
      - FRONTEND_PORT=${FRONTEND_PORT:-3000}
      - PORT=${FRONTEND_PORT:-3000}
      # PixelStreaming connection - frontend connects directly to DRV
      - PIXELSTREAM_URL=ws://${SIM_HOST:-drv-unreal}:${PIXELSTREAM_PORT:-8888}
      - BACKEND_URL=http://backend:${BACKEND_PORT:-5000}
      - REACT_APP_BACKEND_URL=http://${BACKEND_HOST:-localhost}:${BACKEND_PORT:-5000} # For frontend to reach backend
    depends_on:
      - backend
    networks:
      - droneworld-network

  drv-unreal:
    image: droneworld/drv-unreal:v2.1.0
    build:
      context: ./sim  # Build context is now sim directory
      dockerfile: Dockerfile
    env_file:
      - ./.env  # Load environment variables from sim/.env for runtime
    ports:
      - "${SIM_HOST_PORT:-3001}:${SIM_INTERNAL_PORT:-3000}"  # Map internal sim port to host
      - "${PIXELSTREAM_PORT:-8888}:${PIXELSTREAM_PORT:-8888}"  # PixelStreaming - exposed for direct frontend access
    command: 
      - "./DRV.sh"
      - "-server"
      - "-log"
      - "-nullrhi"
      - "-nosound"
      - "-unattended"
      - "-Port=${SIM_INTERNAL_PORT:-3000}"
    environment:
      - DISPLAY=${DISPLAY:-:0}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:ro  # X11 display support
      - drv-data:/app/data  # Persistent data for simulation results
      - type: bind
        source: ${USERPROFILE:-$HOME}/Documents/AirSim
        target: /home/myuser/Documents/AirSim
        read_only: true
    # Uncomment below for GPU access (requires nvidia-docker2)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    networks:
      - droneworld-network
    healthcheck:
      test: ["CMD", "sh", "-c", "netstat -an | grep -q ${SIM_INTERNAL_PORT:-3000} || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  fake-gcs:
    profiles: ["gcs"]
    image: fsouza/fake-gcs-server
    ports:
      - "${FAKE_GCS_PORT:-4443}:${FAKE_GCS_PORT:-4443}"
    command: -scheme http -port ${FAKE_GCS_PORT:-4443} -external-url http://fake-gcs:${FAKE_GCS_PORT:-4443} -log-level error
    volumes:
      - ./fake-gcs-data:/data
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:${FAKE_GCS_PORT:-4443}/storage/v1/b"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - droneworld-network

  init-storage:
    profiles: ["gcs"]
    image: curlimages/curl:latest
    depends_on:
      fake-gcs:
        condition: service_healthy
    command: 
    - sh
    - -c
    - |
      curl -X POST http://fake-gcs:${FAKE_GCS_PORT:-4443}/storage/v1/b \
        -H 'Content-Type: application/json' \
        -d '{"name":"droneworld"}'
    networks:
      - droneworld-network

networks:
  droneworld-network:
    driver: bridge
      
volumes:
  fake-gcs-data:
  drv-data:
